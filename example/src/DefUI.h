// Defines for UI controls


#ifndef FADER
#define FADER(NAME, TITLE, X, Y, R)
#define VSLIDER(NAME, TITLE, X, Y, W, H)
#define BUTTON(NAME, TITLE, X, Y, W, H)
#define LED(NAME, TITLE, X, Y, R)
#define SCREEN(NAME, TITLE, X, Y, W, H)
#endif

